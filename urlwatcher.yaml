dataDir: ./data
watchDir: ./watchers
# onChange: "my-agent --diff-file {{diff}} --instructions-file {{body}}"
defaults:
  htmlConverter: turndown
  jsonConverter: yaml
  timeout: 30000
notifications:
  - type: stdout
