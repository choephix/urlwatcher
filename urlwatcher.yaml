dataDir: ./data
watchDir: ./watchers
# onChange: "my-agent --diff {{diff}} --instructions {{body}}"
defaults:
  htmlConverter: turndown
  jsonConverter: yaml
  timeout: 30000
notifications:
  - type: stdout
